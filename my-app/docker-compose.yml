version: "3.9"

services:
#   mongodb:
#     image: mongo:7
#     container_name: mongodb
#     restart: always
#     ports:
#       - "27017:27017"
#     environment:
#       MONGO_INITDB_ROOT_USERNAME: admin
#       MONGO_INITDB_ROOT_PASSWORD: password123
#     volumes:
#       - mongo_data:/data/db

  valkey:
    image: valkey/valkey:8.0-alpine
    container_name: valkey_server
    restart: always
    ports:
      - "6379:6379"
    command: [ "valkey-server", "--save", "60", "1", "--loglevel", "warning" ]
    volumes:
      - valkey_data:/data

# Optional app container later
#  bullmq-app:
#    build: .
#    container_name: bullmq_service
#    depends_on:
#      - mongodb
#      - valkey

volumes:
  mongo_data:
  valkey_data:
